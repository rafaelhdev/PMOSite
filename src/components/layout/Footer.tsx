export default function Footer() {
  return (
    <footer className="bg-primary-600 text-primary-100 text-center text-xs py-4 mt-auto">
      <p>PMOSite &copy; {new Date().getFullYear()} — Calendário de Férias do Time</p>
    </footer>
  )
}
